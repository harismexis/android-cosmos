<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/mrp_home_row_root_padding">

    <ImageView
        android:id="@+id/imgContainer"
        android:layout_width="@dimen/mrp_home_img_size"
        android:layout_height="@dimen/mrp_home_img_size"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/txtTitle"
        style="@style/MarginTextView"
        app:layout_constraintStart_toEndOf="@+id/imgContainer"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/txtDate"
        style="@style/MarginTextView"
        app:layout_constraintStart_toEndOf="@+id/imgContainer"
        app:layout_constraintTop_toBottomOf="@+id/txtTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>
