<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cosmos">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cosmos">

        <activity android:name=".mrp.activity.MRPDetailActivity" />

        <activity
            android:name=".home.activity.HomeActivity"
            android:label="@string/activity_home_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".mrp.activity.MRPActivity"
            android:label="@string/activity_mrp_label"/>

        <activity
            android:name=".apod.activity.APODActivity"
            android:label="@string/activity_apod_label" />

    </application>

</manifest>